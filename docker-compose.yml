version: '3'
services:
  nginx:
    image: digital-city-nginx
    ports:
      - 80:80
    depends_on:
      - digital-city-auth
    networks:
      - app-network
  digital-city-auth:
    image: digital-city-auth
    ports:
      - 9000:9000
    volumes:
      - ./digital-city-auth/.:/var/www/digital-city-auth
    depends_on:
      - mysql
    networks:
      - app-network
  mysql:
    image: digital-city-mysql
    ports:
      - '3306:3306'
    expose:
      - '3306'
    environment:
      MYSQL_DATABASE: digital_city_auth_db
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - app-network
volumes:
  dbdata:
    driver: local

networks:
  app-network:
    driver: bridge

